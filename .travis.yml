sudo: required
language: ruby
services:
- docker
before_install:
- sudo apt-get -qq update && sudo apt-get install -y --no-install-recommends texlive-full dvipng latex-xcolor pgf
install:
- docker pull egeldenhuys/umlet-docker
script:
- bash ./docs/scripts/build.sh $TRAVIS_BUILD_DIR/docs build